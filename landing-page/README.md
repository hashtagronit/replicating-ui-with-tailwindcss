so in this commit i have done:

1. created navbar component 
2. then imported/init shadcn -neutral theme,  this updates globals.css, created components.json for storing basic settings
3. theen imported shadcn button,  in ui folder by default
